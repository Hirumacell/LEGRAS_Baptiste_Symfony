{% extends 'base.html.twig' %}

{% block title %}supprimer le propriétaire{% endblock %}

{% block body %}
    <div class="container">
        <h1>Supprimer le propriétaire <i>{{ proprietaire.nom }}</i></h1>
        <p class="text-danger">ATTENTION </p>
        <p>Supprimer un propriétaire ne supprime pas les chatons !</p>
        {{ form(formulaire) }}
        {# je vais mettre le bouton retour #}
        <a href="{{ path("app_categories") }}" class="btn btn-primary">Retour</a>
    </div>
{% endblock %}